<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<entry key="selectInstaList">
SELECT
       INSTA_IMG_ORIGIN
     , INSTA_IMG_SRC
  FROM TB_INSTAGRAM
  LEFT JOIN TB_INSTA_IMAGE IMG ON (IMG_NO = INSTA_IMG_NO)
 WHERE DEL_YN = 'N'
 ORDER
    BY UPLOAD_DATE DESC
</entry>
<entry key="likeInstaList">
SELECT 
       INSTA_IMG_ORIGIN
     , INSTA_IMG_SRC
     , COUNT(LIKE_NO)
  FROM TB_INSTAGRAM 
  LEFT JOIN TB_INSTA_IMAGE ON (IMG_NO = INSTA_IMG_NO)
  LEFT JOIN TB_LIKE USING (COM_NO)
 WHERE DEL_YN = 'N'
 GROUP BY INSTA_IMG_ORIGIN
        , INSTA_IMG_SRC
 ORDER BY 3 DESC
</entry>
</properties>
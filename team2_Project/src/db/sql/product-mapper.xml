<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<entry key="categorySearch">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE CATEGORY_NAME = ?
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
</entry>
<entry key="categoryViewSearch">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE CATEGORY_NAME = ?
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
    ORDER BY PRODUCT_VIEW_COUNT DESC 
</entry>
<entry key="categoryRowPrice">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT  
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE CATEGORY_NAME = ?
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
    ORDER BY PRODUCT_PRICE*((100-PRODUCT_DISCOUNT)*0.01)
</entry>
<entry key="categoryHighPrice">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT  
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE CATEGORY_NAME = ?
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
    ORDER BY PRODUCT_PRICE*((100-PRODUCT_DISCOUNT)*0.01) DESC
</entry>
<entry key="productSearch">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE (PRODUCT_NAME LIKE '%' || ? || '%' 
        OR BRAND_NAME = ?)
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
</entry>

<entry key="productViewSearch">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE (PRODUCT_NAME LIKE '%' || ? || '%' 
        OR BRAND_NAME = ?)
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
        ORDER BY PRODUCT_VIEW_COUNT DESC 
</entry>
<entry key="productRowPrice">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE (PRODUCT_NAME LIKE '%' || ? || '%' 
        OR BRAND_NAME = ?)
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
        ORDER BY PRODUCT_PRICE*((100-PRODUCT_DISCOUNT)*0.01)
</entry>
<entry key="productHighPrice">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE (PRODUCT_NAME LIKE '%' || ? || '%' 
        OR BRAND_NAME = ?)
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
        ORDER BY PRODUCT_PRICE*((100-PRODUCT_DISCOUNT)*0.01) DESC
</entry>
<entry key="productDetail">
SELECT 
     PRODUCT_NO
    ,PRODUCT_NAME
    ,PRODUCT_DISCOUNT
    ,PRODUCT_PRICE
    ,BRAND_NAME
    ,PRODUCT_VIEW_COUNT
    ,PRODUCT_IMG_SRC
    FROM TB_PRODUCT 
    JOIN TB_CATEGORY ON(CATEGORY_NO2 = CATEGORY_NO)
    JOIN TB_BRAND USING(BRAND_NO)
    JOIN TB_PRODUCT_IMAGE USING(PRODUCT_NO)
    WHERE PRODUCT_NO = ?
        AND IMG_TYPE = 1
        AND DEL_YN = 'N'
</entry>
<entry key="productViewUp">
UPDATE
    TB_PRODUCT
    SET PRODUCT_VIEW_COUNT = PRODUCT_VIEW_COUNT +1
  WHERE PRODUCT_NO = ?
    AND DEL_YN = 'N'
</entry>
<entry key="selectProductImage">
SELECT *
FROM TB_PRODUCT_IMAGE
WHERE PRODUCT_NO = ?
  AND (IMG_TYPE = 2 OR IMG_TYPE = 3)
</entry>
<entry key="selectProductOption">
SELECT *
    FROM TB_OPTION
    WHERE PRODUCT_NO = ?
</entry>
</properties>